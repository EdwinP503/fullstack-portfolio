This folder contains the archived Express + MongoDB backend sources.

Files:
- server.js
- controllers/
- models/
- routes/
- test/
- jest.config.js
- package.json

If you need to restore this backend, move the contents back to `backend/` and run `npm install`.

Frontend Upgrade Plan (generated 2025-10-23)

Summary of `npm outdated` findings (frontend):
- @fortawesome/fontawesome-free: wanted 6.7.2, latest 7.1.0
- @fortawesome/free-brands-svg-icons: wanted 6.7.2, latest 7.1.0
- @fortawesome/free-solid-svg-icons: wanted 6.7.2, latest 7.1.0
- @fortawesome/react-fontawesome: wanted 0.2.6, latest 3.1.0
- @react-three/drei: wanted 9.122.0, latest 10.7.6
- @react-three/fiber: wanted 8.18.0, latest 9.4.0
- @svgr/webpack: wanted 8.1.0, latest 8.1.0 (up-to-date)
- axios: wanted 1.12.2, latest 1.12.2 (up-to-date)
- emailjs-com: wanted 3.2.0, latest 3.2.0 (up-to-date) — DEPRECATED
- framer-motion: wanted 11.18.2, latest 12.23.24
- react-icons: wanted 5.5.0, latest 5.5.0 (up-to-date)
- react-router-dom: wanted 6.30.1, latest 7.9.4
- react-scripts: wanted 5.0.1, latest 5.0.1 (up-to-date)
- react-swipeable: wanted 7.0.2, latest 7.0.2 (up-to-date)
- styled-components: wanted 6.1.19, latest 6.1.19 (up-to-date)
- three: wanted 0.169.0, latest 0.180.0
- webpack-dev-server: wanted 5.2.2, latest 5.2.2 (up-to-date)

Observations:
- Major jumps: FontAwesome (6.x -> 7.x), @fortawesome/react-fontawesome (0.x -> 3.x), @react-three/fiber/drei and three, framer-motion, react-router-dom (6 -> 7).
- `emailjs-com` is deprecated; recommend removing if unused (no imports found).
- `react-swipeable` is used in `frontend/src/components/ProjectPage.js` and should be kept.

Recommended upgrade strategy (safe, stepwise):

1) Prep
- Create a branch: `git checkout -b upgrade/frontend-deps`
- Commit current `frontend/package.json` and `frontend/package-lock.json`.
- Ensure dev flow reproducible: `npm run build` and `npm start` locally for smoke tests.

2) Low-risk updates (apply first)
- In `/frontend` run:
  ```bash
  npm update
  npm install
  npm run build
  ```
- Purpose: update to the "wanted" versions inside current semver ranges (minor/patch).
- Test: start the app and smoke-test key flows (routing, contact form, 3D scenes).

3) Remove deprecated/unused
- Remove `emailjs-com` if truly unused:
  - Confirm no imports; then run:
    ```bash
    npm uninstall emailjs-com
    npm install
    npm run build
    ```
  - If you want EmailJS, replace with `@emailjs/browser` instead.

4) Major upgrades (one group at a time, with tests)
- react-three / three group:
  - Upgrade `three`, `@react-three/fiber`, `@react-three/drei` together to compatible latest versions.
  - Update any API changes in 3D code; consult library changelogs.
  - Commands:
    ```bash
    npm install three@latest @react-three/fiber@latest @react-three/drei@latest
    npm run build
    ```
- react-router-dom:
  - Upgrade to v7 after reviewing migration notes.
  - Update code where router hooks/APIs changed.
  - Commands:
    ```bash
    npm install react-router-dom@latest
    npm run build
    ```
- framer-motion:
  - Upgrade and verify animations.
    ```bash
    npm install framer-motion@latest
    npm run build
    ```
- FontAwesome packages:
  - Major version bump; update imports/usages according to changelog.
    ```bash
    npm install @fortawesome/fontawesome-free@latest @fortawesome/free-solid-svg-icons@latest @fortawesome/free-brands-svg-icons@latest @fortawesome/react-fontawesome@latest
    npm run build
    ```

5) After each upgrade
- Run `npm run build` and thorough local QA.
- Run `npm audit` and fix critical vulnerabilities.
- Commit changes to the upgrade branch.

6) Suggested ordering (risk-based)
- A: `npm update` (minor/patch)
- B: Remove `emailjs-com` if unused
- C: Upgrade `three` + `@react-three/fiber` + `@react-three/drei` together
- D: Upgrade `react-router-dom` to v7
- E: Upgrade `framer-motion`
- F: Upgrade FontAwesome packages
- G: Final smoke tests, `npm audit`, commit and open PR

Estimated effort:
- `npm update` + build/test: ~10–20 minutes
- Major upgrades (react-router, react-three, fontawesome): 1–3 hours depending on code changes

Notes & cautions:
- Always run builds and test 3D scenes and routing after each step.
- Keep changes isolated in a branch and one upgrade per PR where possible.

Generated by automated scan on 2025-10-23.

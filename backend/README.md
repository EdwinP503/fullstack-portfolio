Active backend: AWS Lambda (DynamoDB)

This repository's backend has been switched to an AWS Lambda-based handler located in AWS.

Archived legacy Express + MongoDB backend is available in `backend/archive/mongodb-backend/` (preserved for reference).
